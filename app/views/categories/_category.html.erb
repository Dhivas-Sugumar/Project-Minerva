<div id="<%= dom_id category %>">
  <h1><%=category.category %></h1>
  <%@course_category = category.courses%>
  <%@course_category.each do |course|%>
    <%= render partial: "courses/course_card", locals: {course: course} %>
<% end %>


</div>
