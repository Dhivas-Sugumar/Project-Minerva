
<%= bootstrap_form_for(@category) do |f| %>

  <div class="form-inputs">
    <%= f.text_area :category %>
  </div>

  <div class="form-inputs">
    <%= f.file_field :banner %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
